
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.solidcolorwallpaperstudio',
  appName: 'solid-color-wallpaper-studio',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
